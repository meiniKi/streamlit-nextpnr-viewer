import React from "react"
import ReactDOM from "react-dom"
import NextpnrViewer from "./NextpnrViewer"

ReactDOM.render(
  <React.StrictMode>
    <NextpnrViewer />
  </React.StrictMode>,
  document.getElementById("root")
)
